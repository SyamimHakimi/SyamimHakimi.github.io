rules_version = '2';

// Firestore security rules — Phase A6 will harden these.
// Currently open for reads (portfolio content is public) and closed for writes.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
