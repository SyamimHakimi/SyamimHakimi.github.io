# Environment variables (never commit)
.env

# Build output and caches
dist
dist-ssr
.astro/

# Dependencies
node_modules
coverage

# UI preview temp files
scripts/test-mockup.html
scripts/*.png

# Logs and CI artifacts
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
merge-logs*/
*.zip

# OS / editor artifacts
.DS_Store
.claude/
*.local
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# GitHub App private key (never commit)
scripts/*.pem
scripts/agents/*.pem

# Firebase service account credentials (never commit — contains private key)
*firebase-adminsdk*.json
*service-account*.json
serviceAccountKey.json
*serviceAccount*.json
