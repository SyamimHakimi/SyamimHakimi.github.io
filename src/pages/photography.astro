---
/**
 * Full gallery — route `/photography`.
 * Islands: GalleryGrid.vue + GalleryLightbox.vue (client:visible) — Phase A5
 * Composable: useGallery()
 */
import PageLayout from "../layouts/PageLayout.astro";
---

<PageLayout
  title="Photography — syamim."
  description="Syamim Hakimi's photography gallery — favourite shots sorted by date."
>
  <div class="mx-auto max-w-6xl px-4 py-12 sm:px-6">
    <header class="mb-10">
      <h1 class="text-3xl font-bold sm:text-4xl">Gallery</h1>
      <p class="mt-2 text-[var(--color-text-muted)]">
        Favourite frames — film simulations on a Fujifilm X-E4.
      </p>
    </header>

    <section aria-label="Photography Gallery">
      <!--
        GalleryGrid island mounted here in Phase A5.
        Uses: useGallery() — paginated, favourite only, ordered by date desc.
        GalleryLightbox island also mounted here for full-screen viewing.
      -->
      <p class="text-[var(--color-text-muted)]">Gallery loading…</p>
    </section>
  </div>
</PageLayout>
